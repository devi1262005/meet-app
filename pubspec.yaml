name: meet
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.3.0
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  firebase_storage: ^12.4.4
  flutter_native_splash: ^2.4.1
  animated_splash_screen: ^1.3.0
  firebase_core: ^3.4.0  # Updated to a compatible version with firebase_auth and cloud_firestore
  firebase_auth: ^5.2.0   # Compatible with firebase_core ^3.4.0
  google_sign_in: ^6.1.0
  font_awesome_flutter: ^10.6.0
  speech_to_text: ^7.0.0
  image_picker: ^1.0.0
  fluttertoast: ^8.2.1
  flutter_webrtc:
    path: plugins/flutter_webrtc
  http: ^1.2.2
  path_provider: ^2.0.11
  intl: ^0.20.2
  permission_handler: ^12.0.0+1
  cached_network_image: ^3.2.0
  flutter_sound: ^9.6.0
  socket_io_client: ^3.1.1
  connectivity_plus: ^6.1.3
  pdf: ^3.10.7
  printing: ^5.11.1
  file_picker: ^10.1.2
  shared_preferences: ^2.2.2
  http_parser: ^4.0.2
  # Latest flutter_background from pub.dev; verify the version on pub.dev for updates.
  flutter_background: ^1.0.10
  # Background execution package – verify latest version on pub.dev
  share_plus: ^10.1.4




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

